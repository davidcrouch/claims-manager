use_default_settings: true

general:
  instance_name: "more0ai-search"

search:
  formats:
    - html
    - json

server:
  secret_key: "more0ai-dev-searxng-secret-key-change-in-prod"
  limiter: false
