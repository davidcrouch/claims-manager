import { ComingSoon } from '@/components/shared/ComingSoon';

export const metadata = { title: 'Messages — EnsureOS' };

export default function MessagesPage() {
  return (
    <ComingSoon
      title="Messages"
      description="The cross-entity message inbox is coming soon. You'll see all messages and attachments across jobs and claims in one place."
    />
  );
}
