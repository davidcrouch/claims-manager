/** Default form drawer width (BottomFormDrawer). */
export const FORM_DRAWER_WIDTH_CLASS = 'w-[70%]';

/**
 * Max form width while companion chat is open.
 * Forms wider than this shrink so chat can keep its minimum share.
 */
export const FORM_DRAWER_BESIDE_CHAT_WIDTH_CLASS = 'w-[60%]';

/**
 * Chat drawer width when opened beside an open form drawer.
 * Never less than 40% of the viewport.
 */
export const CHAT_BESIDE_FORM_WIDTH_CLASS = 'w-[40%]!';

/** Left inset for a right-side form backdrop while companion chat is open on the left. */
export const FORM_DRAWER_COMPANION_LEFT_CLASS = 'left-[40%]';

/** Right inset for a left-side form backdrop while companion chat is open on the right. */
export const FORM_DRAWER_COMPANION_RIGHT_CLASS = 'right-[40%]';
