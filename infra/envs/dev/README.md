# Moved

Pub/Sub + secrets for the DEV GCP project live under:

**[`deploy/terraform/environments/dev`](../../../deploy/terraform/environments/dev)**

That root follows the data_cloud hybrid pattern (topics/secrets/GCS in
`claims-manager-dev-493807`; app runtime stays local).
